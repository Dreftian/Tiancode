## Plan: Limpieza segura del frontend (app, web, desktop, ui, tui)

Refactor de bajo riesgo sobre hallazgos verificados de la auditoría. No cambia comportamiento funcional; cada fase se valida con typecheck + tests de los paquetes afectados.

### Fase 1 — Config raíz y código muerto
1. **Arreglar `.oxlintrc.json`**: la clave `options` está duplicada 3 veces (líneas 3, 44, 47) — fusionar en una sola sin cambiar reglas.
2. **Eliminar código muerto en `backend/ui`** (verificado: 0 imports en todo el repo):
   - `src/components/typewriter.tsx` (+ su `.css` y `.stories.tsx` si existen)
   - `src/components/thinking-heading.stories.tsx` (854 líneas; el "componente" solo existe dentro del archivo de stories)
3. **Quitar bloques de código comentado en `backend/app`** (8 sitios):
   - `components/edit-project.ts:85`, `components/settings-general.tsx:135`, `components/terminal.tsx:579,616`, `context/global-sync/event-reducer.ts:215-224`, `pages/layout.tsx:1307`, `utils/server-compat.ts:187,312-313`, `context/server-session.ts:960-961`

### Fase 2 — Eliminar tipos `any`
4. **`frontend/ui/src/components/scroll-view.tsx`** (líneas 355-374): reemplazar los ~10 casts `as any` con firmas de handlers tipadas (los eventos ya se tipan al propagarlos).
5. **`frontend/ui/src/components/popover.tsx:144`**: tipar el spread de `triggerProps`.
6. **`backend/app`**: `context/global.tsx:52`, `context/global-sync/bootstrap.ts:538`, `pages/session/file-tabs.tsx:496,787`, `utils/persist.ts:14`.
7. **`backend/desktop`** (2) y **`backend/tui`** (6): eliminar los `any` restantes con tipos concretos.

### Fase 3 — Deduplicar helpers del proceso main de desktop
8. `ensureLoopbackNoProxy`/`useEnvProxy` están duplicados en `main/index.ts` y `main/sidecar.ts`; `serializeError` en `main/server.ts` y `main/sidecar.ts`. Extraer a un módulo compartido nuevo `frontend/desktop/src/main/util/proxy.ts` y `frontend/desktop/src/main/util/error.ts`, actualizar imports y cubrir con tests (ya hay 14 archivos de test en el paquete).

### Fase 4 — Scripts y tooling
9. Añadir script `lint` (oxlint con la config raíz) a `app`, `web`, `desktop`, `ui`, `tui`.
10. Añadir `typecheck` y `test` a `backend/web` (hoy no tiene ninguno).
11. (Solo si no rompe CI) habilitar categoría `correctness` en `.oxlintrc.json` para el frontend y corregir lo que salga.

### Validación
- `bun run typecheck` desde cada paquete tocado (`ui`, `app`, `desktop`, `tui`, `web`)
- `bun test` desde `backend/ui`, `backend/desktop`, `backend/app` (regla AGENTS.md: nunca desde la raíz)
- `oxlint` desde la raíz al final
- Los TODOs de `tui/src/util/parsers-config.ts` (tree-sitter) son bugs funcionales, no limpieza — quedan documentados como pendiente, fuera de este pase.

### Fuera de alcance (requieren decisión/diseño aparte)
- Migración v1→v2 en app (`terminal-panel` vs `-v2`, `layout.tsx` vs `layout-new.tsx`, `legacy-home`)
- Consolidar las 2 librerías DnD (`@dnd-kit/*` vs `@thisbeyond/solid-dnd`)
- Dividir archivos de 1700-2700 líneas
- Unificar `formatDuration`/storage entre tui/app/web (tui no puede depender de ui por la dirección de dependencias)
